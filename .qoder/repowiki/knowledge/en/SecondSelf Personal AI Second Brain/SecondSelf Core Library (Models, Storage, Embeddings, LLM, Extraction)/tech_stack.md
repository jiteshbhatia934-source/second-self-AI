Python dataclasses for models; sentence-transformers (`all-MiniLM-L6-v2`) for 384-dim embeddings with numpy cosine similarity; Groq API (`llama-3.1-8b-instant`) for LLM calls with dotenv-based `GROQ_API_KEY`; BeautifulSoup + requests for web scraping; pypdf for PDF text extraction; PyYAML (with line-parser fallback) for markdown front-matter parsing; pickle for embedding persistence.